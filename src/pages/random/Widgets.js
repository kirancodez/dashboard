import React from "react";
import Base from "../Base";

const Widgets = () => {
    return(
        <Base title="Widgets">
            <h3 className="text-secondary text-center mt-5"> - No contens -</h3>
        </Base>
    )
}

export default Widgets;